class movie():
    def __init__(self, title, main character, genre, director, composer):
        self.title = title
        self.maincharacter = self.maincharacter
        self.genre = self.genre
        self.director = self.director
        self.composer = self.composer

        def title_sequence(self, title, maincharacter):
            print("Movie opens with" + self.title + "Spider-Man")

        def fight_scene(self, genre, maincharacter):
            print("Movie" + "shows" + "Spider-Man fighting Green Goblin")

        def opening_theme(self, composer):
            print("Danny Elfmann" + "composes" + "opening theme song")
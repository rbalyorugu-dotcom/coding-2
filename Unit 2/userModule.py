class CarModel:
    def __init__(self, color, model, speed):
        self.color = color
        self.model = model
        self.speed = speed

        def drive(self, model, speed):
            print(self.model + "is driving at" + self.speed + "50 mph"):

        def color(self, color, model):
            print(self.model + "is" + "red")

        def brake(self, model):
            if brakes are pushed:
                print(self.model + "is stopping")

class CarModel(Ferrari f80)